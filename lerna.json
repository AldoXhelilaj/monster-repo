{
  "packages": ["packages/*"],
  "version": "independent",   

    
  "command": {
    "publish": {
      "ignoreChanges": ["**/*.test.ts", "**/*.spec.ts"], 
      "message": "chore(release): publish",
      "registry": "https://registry.npmjs.org/"
    }
  }
}
